# Internal design docs. Do not publish. Anchored so fixture bundles keep their own copies.
/SDD.md
/BUILD.md
# The decisions about SDD.md belong to SDD.md, which a clone does not hold.
/.speccy/decisions/SDD.md.yaml

/bin/
/build/
# `go build ./tools/buildtool` from the root writes the binary here.
/buildtool
node_modules/
web/dist/*
!web/dist/.keep
*.tsbuildinfo
.DS_Store
.speccy/state/

# Third-party reference screenshots for the UI gauntlet (BUILD.md §6.1). Kept local only.
docs/gauntlet/reference/

# goreleaser output (just release-check).
/dist/
